# Personal config — contains your VLAN definitions, IP addresses, NIC selection
vlan_config.json

# Runtime output — contains ping results and IP addresses
vlan_results.json
vlan_matrix.txt

# PyInstaller build artifacts
build/
dist/
*.spec

# MSIX build artifacts
VLANPackage/
*.msix
*.msixupload

# Visual Studio
VLANReachabilityTester/

# Claude Code memory (local session data)
.claude/

# Compiled executable — distributed via GitHub Releases, not committed
*.exe

# Portable-mode sentinel — created by `build.ps1 -Demo` alongside the demo exe
portable.flag

# Python cache
__pycache__/
*.pyc
*.pyo
